version: '3.8'

services:
  notes-app:
    image: ghcr.io/dtorrero/notes:latest
    container_name: simple-notes-app
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped
